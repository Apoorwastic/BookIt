# BookIt Frontend

## Setup
1. Copy `.env.example` to `.env` and set `VITE_API_URL` to your backend URL (e.g. http://localhost:5000)
2. Install:
   ```
   cd frontend
   npm install
   ```
3. Run:
   ```
   npm run dev
   ```
