This frontend is a minimal Vite + React + TypeScript project with Tailwind CSS.
After installing dependencies, run `npm run dev`. Ensure backend is running and
VITE_API_URL points to the backend base URL.
